ROTATION = 360;  // degrees
TRIANGLE_DEGREES = 180;

ORIGIN = [0, 0, 0];
ORIGIN_2D = [0, 0];

EPSILON = 0.01; // used to prevent zero-width gaps & the like

T_SIDES = 0; 
T_ANGLES = 1;

// TODO layer height, tolerance, etc
